import React from 'react'

const Question = ({background,index}) => {
  return (
    <div  >
       <div style={{width: '100%',display:'flex',flexDirection:'row', height: '100%', position: 'relative', background: 'rgba(152.75, 174.15, 249.05, 0.50)', borderRadius: 10}} >
        <div>1.</div>
        <div>
        Question bled it to make a type specimen book.
        </div>
       </div>
    </div>
  )
}

export default Question
